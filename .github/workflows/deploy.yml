name: Deploy Slate Docs

on:
  push:
    branches:
      - deploy-test  # Change to your default branch if necessary

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2.6'  # Ensure this matches your Ruby version
          bundler-cache: true

      - name: Install Bundler 2.4.19
        run: gem install bundler -v 2.4.19

      - name: Install dependencies
        run: |
          ls -la  # Debugging step to check files
          bundle config set --local path 'vendor/bundle'
          bundle install

      - name: Verify Installed Gems
        run: bundle list

      - name: Build documentation
        run: bundle exec middleman build --clean

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build  